name: Deploy App

on:
  workflow_dispatch:
    inputs:
      app_version:
        description: 'App version to deploy'
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps: # Correct indentation for 'steps'
      - name: Checkout code  # Correct indentation for steps within 'steps'
        uses: actions/checkout@v3

      - name: Print app version # Correct indentation
        run: | 
          echo "Deploying app version: ${{github.event.inputs.app_version}}"

      - name: Deploy app # Correct indentation
        run: | # Correct indentation
          # Add your deployment logic here
          echo "Deploying version ${{ github.event.inputs.app_version }}"
          # Example: Use the version in a deployment script
          # ./deploy.sh '${{ github.event.inputs.app_version }}'
